# yaml-language-server: $schema=https://raw.githubusercontent.com/microsoft/waza/main/schemas/eval.schema.json
name: odoo-rag-evaluation-trigger-eval
description: Offline trigger checks for the repository-scoped Wiki RAG evaluation skill.
skill: odoo-rag-evaluation
schemaVersion: "1.0"
version: "1.0"
config:
  trials_per_task: 1
  timeout_seconds: 60
  executor: mock
  model: mock
  parallel: false
  inject_skill_body: true
  skill_directories:
    - .agents/skills/odoo-rag-evaluation
  required_skills:
    - odoo-rag-evaluation
metrics:
  - name: trigger_accuracy
    weight: 1.0
    threshold: 1.0
    description: Positive and negative prompts must route to the intended skill boundary.
tasks:
  - tasks/*.yaml
