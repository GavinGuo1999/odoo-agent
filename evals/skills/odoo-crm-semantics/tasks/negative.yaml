# yaml-language-server: $schema=https://raw.githubusercontent.com/microsoft/waza/main/schemas/task.schema.json
id: crm-semantics-negative
name: Sales order analytics should not trigger
description: Sales-order metric work belongs to the workflow development skill, not CRM semantics.
inputs:
  prompt: Optimize the sale_order monthly revenue trend SQL prompt and its Chart Planner fallback.
expected:
  should_trigger: false
graders:
  - type: trigger
    name: crm_semantics_negative_trigger
    config:
      skill_path: .agents/skills/odoo-crm-semantics/SKILL.md
      mode: negative
      threshold: 0.55
